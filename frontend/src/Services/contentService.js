import React,{useState,useEffect} from 'react';
import axios from 'axios';

export default function tutorialService(props) {
	const [data, setData] = useState({});
	const query=props.query;
	useEffect(() => {
		return () => {
			effect
		};
	}, [input])
	

}